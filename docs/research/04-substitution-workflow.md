# 04 — substitution workflow

Evidence: the referenced conversation describes a supplied spreadsheet/GAS workflow, but no authoritative project source was imported into this Phase 0 workspace.

Useful boundary: semester generation and day-to-day leave/substitution transactions have different lifecycles and actors. A kernel may later serve both, but they should not be conflated now.

Phase 0 action: explicitly out of scope. No notification, approval, confirmation-code, attendance, or substitute-payment objects are introduced.

